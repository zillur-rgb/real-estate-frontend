import { VStack } from "@chakra-ui/react";
import HeadingComponent from "../shared/Heading";

const ListedProperties = () => {
  return (
    <VStack>
      <HeadingComponent text="You have omuk number of properties" />
    </VStack>
  );
};

export default ListedProperties;
